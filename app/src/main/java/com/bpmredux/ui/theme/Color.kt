package com.bpmredux.ui.theme

import androidx.compose.ui.graphics.Color

val Black = Color(0xFF000000)
val Surface = Color(0xFF030808)
val SurfaceVariant = Color(0xFF0A1414)

// Teal monochrome palette — BR2049 terminal aesthetic
val Accent = Color(0xFF00E5FF)         // bright cyan — active/highlighted
val AccentDim = Color(0xFF00889A)      // glow teal — phosphor bloom
val AccentSubtle = Color(0xFF0D2A2D)   // very dark teal — active fills

val TextPrimary = Color(0xFF7FCFD6)    // soft teal — primary text
val TextSecondary = Color(0xFF2A6B6E)  // dark teal — inactive labels
val TextDim = Color(0xFF163538)        // very dim teal — borders, bg elements
